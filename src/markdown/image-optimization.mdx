Optimizing images is crucial for web performance, user experience, and SEO.

## If you use Next.js: Using the `<Image />` Component

Next.js provides a built-in `<Image />` component that offers several benefits over the standard HTML `<img>` tag:

**1. Automatic Optimization:** Images are automatically resized, optimized, and served in modern formats (like WebP) when possible.

<MdxImage src="/blog/image-optimization_1.png" alt="image-optimization_1" />

<MdxImage src="/blog/image-optimization_2.png" alt="image-optimization_2" />

<MdxImage src="/blog/image-optimization_3.png" alt="image-optimization_3" />

In this example, I compare an image rendered with the `<Image />` component to one rendered with a standard `<img />` tag. You can see that the file type and size are different, and the image is compressed.

**2. Lazy Loading:** Images are loaded only when they enter the viewport, improving initial page load speed.

**3. Image Placeholder:** Allow to use a blurred placeholder while your image loading.

This technique is especially useful for large images or slow network conditions, making your site feel faster and more polished.

```js
<Image
  src={"https://remote-image.com/image.jpg"}
  alt="remote-image"
  height={size}
  width={size}
  placeholder="blur"
  blurDataURL="/loading.jpg"
/>
```

---

## If you use original React.js:

**Bonus**

| File Type | Use Case               | Pros                       | Cons                  |
| --------- | ---------------------- | -------------------------- | --------------------- |
| WebP      | Photos, graphics       | Small size, good quality   | Not in IE/Safari < 14 |
| JPEG      | Photos                 | Good quality, wide support | Larger than WebP      |
| PNG       | Graphics, transparency | Lossless, transparency     | Large file size       |
| SVG       | Icons, logos, graphics | Scalable, tiny size        | Not for photos        |

---

**Conclusion:**

- Use Next.js `<Image />` for automatic, best-in-class optimization.
- In React, prefer WebP with JPEG/PNG fallbacks, and always compress images.
- Use SVG for vector graphics.
